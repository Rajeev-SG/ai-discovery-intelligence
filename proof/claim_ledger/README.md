# Claim ledger proof artefacts

Generated by `scripts/build_claim_proof.py`; do not edit by hand.

| File | Contents |
| --- | --- |
| `PROOF.md` | Rendered expanded observation/evidence view for the four real claims |
| `claim_specs.json` | Extraction-rule bundle: every value names the exact quote/selector supporting it |
| `captures.json` | Capture manifest: URL, HTTP status, byte size, `raw_sha256`, fetch time |
| `expanded_claims.json` | Machine-readable form of the rendered ledger rows |
| `captures/*.html` | Raw publisher HTML - **private, git-ignored**; hashes are committed |

The captures come from live public pages fetched respecting `robots.txt`, with no
bypass. Raw HTML is kept out of git; the manifest hashes are what make the rendering
checkable.
